// Main application logic for the uxai-ugc-agent web dashboard

document.addEventListener('DOMContentLoaded', () => {
    console.log('uxai-ugc-agent dashboard loaded');

    const startBtn = document.getElementById('start-btn');
    startBtn.addEventListener('click', () => {
        alert('Pipeline start requested (logic not implemented yet)');
    });
});
